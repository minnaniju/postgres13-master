users:
  - name: liquibase
    password: $POSTGRESQL_LIQUIBASE_PASSWORD
    role: superuser
databases:
  - name: liquibase
    owner: liquibase
    tablespace:
      - name: tb1
        location: /appz/data/tb1
    schemas:
      - name: schema1
        authorised_user: liquibase
        search_path: true
  
